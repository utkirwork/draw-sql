<?php

namespace {{namespace}}\modules\api\controllers;

use {{namespace}}\models\\{{modelName}};
use {{namespace}}\models\search\\{{modelName}}Search;
use {{namespace}}\actions\\{{modelNameLower}}\Create{{modelName}}Action;
use {{namespace}}\actions\\{{modelNameLower}}\Update{{modelName}}Action;
use {{namespace}}\actions\\{{modelNameLower}}\Delete{{modelName}}Action;
use {{namespace}}\forms\\{{modelNameLower}}\Create{{modelName}}Form;
use {{namespace}}\forms\\{{modelNameLower}}\Update{{modelName}}Form;
use {{namespace}}\service\\{{modelName}}Service;
use yii\rest\ActiveController;
use yii\web\NotFoundHttpException;
use yii\filters\VerbFilter;
use yii\rest\IndexAction;
use yii\rest\ViewAction;

/**
 * {{modelName}} Controller
 * 
 * @OA\Tag(
 *     name="{{modelNameLower}}",
 *     description="{{modelName}} management"
 * )
 */
class {{modelName}}Controller extends ActiveController
{
    public $modelClass = {{modelName}}::class;

    /**
     * @inheritdoc
     */
    public function actions()
    {
        return [
            'view' => [
                'class' => ViewAction::class,
                'modelClass' => $this->modelClass,
            ],
            'create' => [
                'class' => Create{{modelName}}Action::class
            ],
            'update' => [
                'class' => Update{{modelName}}Action::class
            ],
            'delete' => [
                'class' => Delete{{modelName}}Action::class
            ],
            'index' => [
                'class' => IndexAction::class,
                'modelClass' => $this->modelClass,
                'dataFilter' => [
                    'class' => \yii\data\ActiveDataFilter::class,
                    'searchModel' => {{modelName}}Search::class
                ]
            ],
            'options' => [
                'class' => 'yii\rest\OptionsAction',
                'resourceOptions' => ['GET', 'PUT', 'PATCH', 'DELETE', 'HEAD', 'OPTIONS', 'POST'],
                'collectionOptions' => ['GET', 'PUT', 'PATCH', 'DELETE', 'HEAD', 'OPTIONS', 'POST'],
            ],
        ];
    }

    /**
     * @inheritdoc
     */
    public function behaviors()
    {
        $behaviors = parent::behaviors();
        
        $behaviors['verbs'] = [
            'class' => VerbFilter::class,
            'actions' => [
                'index' => ['GET', 'HEAD', 'OPTIONS'],
                'view' => ['GET', 'HEAD', 'OPTIONS'],
                'create' => ['POST', 'OPTIONS'],
                'update' => ['PUT', 'PATCH', 'OPTIONS'],
                'delete' => ['DELETE', 'OPTIONS'],
                'options' => ['OPTIONS'],
            ],
        ];

        return $behaviors;
    }

    /**
     * Finds the {{modelName}} model based on its primary key value.
     * If the model is not found, a 404 HTTP exception will be thrown.
     * @param integer $id
     * @return {{modelName}} the loaded model
     * @throws NotFoundHttpException if the model cannot be found
     */
    protected function findModel($id)
    {
        if (($model = {{modelName}}::findOne($id)) !== null) {
            return $model;
        }

        throw new NotFoundHttpException('The requested page does not exist.');
    }
} 